<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Custom" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/night_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:navigationBarColor">@color/night_700</item>
        <item name="android:windowBackground">@color/night_window_background</item>
        <!-- Secondary brand color. -->

         Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
    </style>
</resources>